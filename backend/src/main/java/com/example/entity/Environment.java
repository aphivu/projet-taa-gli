package com.example.entity;

/**
 * Environment enum Type to handle weather analysis
 * For now we only look if it is raining and we compare if sport is outside
 */

public enum Environment {
    INSIDE,
    OUTSIDE
}
